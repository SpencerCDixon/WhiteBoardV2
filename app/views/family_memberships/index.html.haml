%h2 Invite Family Members
= form_for @invitation do |f|
  - if @invitation.errors.any?
    %ul
      - @invitation.errors.full_messages.each do |error|
        %li= error

  = f.label :email
  = f.text_field :email

  = f.label :name
  = f.text_field :name

  = f.submit 'Send invite', class: "button success tiny"

%h2 Family Members
%table.table-minimal
  %thead
    %tr
      %th Name
      %th Email
  %tbody
    - @members.each do |member|
      %tr
        %td= member.name
        %td= member.email



